def solution(N):
#convert int into binary, strip trailing zeros, split at '1' to list, then find longest element in list and get this element lenght.
return len(max(format(N, 'b').strip('0').split('1')))  